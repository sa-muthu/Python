# Area of circle = pi r^2

import math

radius = float(input("Enter the radius of the circle: "))
Area = math.pi * math.pow(radius, 2)

print(f"Area of the given circle of radius {radius}cm = {Area}")